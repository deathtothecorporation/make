import { Poke } from '../lib';
import { Belt, Task, SessionTask } from './types';
export declare const pokeTask: (session: string, task: Task) => Poke<SessionTask>;
export declare const pokeBelt: (session: string, belt: Belt) => Poke<SessionTask>;
export declare const scrySessions: () => string[];
